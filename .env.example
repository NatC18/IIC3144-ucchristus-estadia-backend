# Archivo de variables de entorno para desarrollo
# Copia este archivo a .env y modifica los valores según tu configuración
# IMPORTANTE: NUNCA commits archivos .env con credenciales reales

# Django Settings
DEBUG=1
SECRET_KEY=django-insecure-development-key-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Base de Datos PostgreSQL
DB_NAME=ucchristus_db
DB_USER=ucchristus_user
DB_PASSWORD=ucchristus_secure_password
DB_HOST=db
DB_PORT=5432

# Auth0 Configuration
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_AUDIENCE=your-api-audience
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret

# Redis (opcional)
REDIS_URL=redis://redis:6379/0

# Configuración de Producción (para Render, Railway, etc.)
DATABASE_URL=postgresql://user:password@host:5432/database
RENDER_EXTERNAL_HOSTNAME=your-app.onrender.com

# Notas de Seguridad:
# 1. Genera un SECRET_KEY único: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 2. Usa contraseñas fuertes para la base de datos
# 3. Obtén las credenciales Auth0 de tu dashboard
# 4. Nunca compartas estos valores en repositorios públicos

# Para Deploy en Render:
# - ALLOWED_HOSTS=*.onrender.com